{
  "name": "bolg-components",
  "license": "MIT",
  "module": "dist/index.js",
  "main": "dist/index.js",
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -cw",
    "test": "jest src",
    "test:watch": "jest src --watch",
    "serve": "live-server public --mount=/dist:./dist",
    "preview": "concurrently npm:serve npm:watch",
    "prepublishOnly": "npm run build",
    "format": "prettier --write **/*.ts"
  },
  "dependencies": {
    "lit": "^2.0.0-rc.3"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^13.0.4",
    "@rollup/plugin-typescript": "^8.2.5",
    "concurrently": "^6.2.1",
    "husky": "^7.0.1",
    "lint-staged": "^11.1.2",
    "live-server": "^1.2.1",
    "prettier": "^2.3.2",
    "rollup": "^2.56.3",
    "ts-lit-plugin": "^1.2.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Joe-noh/bolg-components"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "**/*.ts": [
      "prettier --write",
      "git add"
    ]
  }
}
